# ──────────────────────────────────────────────────────────────
# 服务注册地址（Docker 环境默认值）
# 本地调试通过 application-dev.yml 覆盖
# 生产环境通过 application-prod.yml 覆盖
# ──────────────────────────────────────────────────────────────
zyn:
  discovery:
    services:
      sys: http://zyn-sys:8081
      netty: http://zyn-netty:18081
